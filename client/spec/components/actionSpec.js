/* eslint-disable */
describe('Actions', () => {

  describe('Event' , () => {
    xit('Should be able to Add an event', ()=> {

    });

    xit('Should not mutate the state when adding event', ()=> {

    });
  });

  describe('Voting', () => {
    xit('Should be able to remove an event', ()=> {

    });

    xit('Should not mutate the state when removing an event', ()=> {

    });
  });

  describe('New Address', () => {
    xit('Should be able to add an event address', ()=> {

    });

    xit('Should not mutate the state when adding an event address', ()=> {

    });
  });


});

describe('View filters', () => {
  describe('Timeline Lookup', () => {
    xit('Should return a state with queried timeline', () => {

    });

    xit('Should not mutate the state and return a new object', ()=> {

    });
  });
  describe('Create Event', () => {
    xit('Should return the state', () => {

    });
  });
});
